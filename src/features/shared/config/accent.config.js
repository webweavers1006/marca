/**
 * Global configuration for the decorative Accent Characters (Watermarks).
 * These "ghost" characters are used across modules to reinforce brand identity.
 */
export const ACCENT_CONFIG = {
  /** Default text to render if none is provided */
  defaultChar: "VE",

  /** Default opacity class to ensure it remains a subtle watermark */
  defaultOpacity: "opacity-10",
};
